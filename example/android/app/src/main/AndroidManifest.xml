<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.takeoffmediareactnativebitmovinplayer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:theme="@style/AppTheme">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:supportsPictureInPicture="true"
        android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode|smallestScreenSize|screenLayout"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <meta-data android:name="BITMOVIN_PLAYER_LICENSE_KEY" android:value="@string/BITMOVIN_PLAYER_LICENSE_KEY"/>
      <meta-data android:name="BITMOVIN_ANALYTICS_LICENSE_KEY" android:value="@string/BITMOVIN_ANALYTICS_LICENSE_KEY"/>
      <meta-data android:name="BITMOVIN_PLAYER_CSS" android:value="@string/BITMOVIN_PLAYER_CSS"/>
      <meta-data android:name="BITMOVIN_PLAYER_JS" android:value="@string/BITMOVIN_PLAYER_JS"/>
      <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" />
    </application>

</manifest>
